import OptionBuffer from "#models/optionBuffer";
import BaseService from "#services/base";

class OptionBufferService extends BaseService {
  static Model = OptionBuffer;
}

export default OptionBufferService;
